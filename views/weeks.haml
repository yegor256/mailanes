%h2
  Weekly

%p
  This is your weekly contribution to the
  = succeed ':' do
    %a{href: "/add?list=#{list.id}"}
      = "List ##{list.id}"
  = list.title

%table
  %thead
    %tr
      %th Week
      %th Added
      %th Bounced
      %th Valid
  %tbody
  - weeks.each do |w|
    %tr
      %th= w[:name]
      %th{style: 'text-align:right'}= sprintf('%+d', w[:total])
      %th{style: 'text-align:right'}= sprintf('%+d', w[:bad])
      %th{style: 'text-align:right'}= sprintf('%+d', w[:total] - w[:bad])

