%h2
  Delivery
  = succeed ':' do
    = "##{delivery.id}"

%p
  = succeed ':' do
    %a{href: "/list?id=#{delivery.recipient.id}"}= "Recipient ##{delivery.recipient.id}"
  = "\"#{delivery.recipient.email}\""

%p
  = succeed ':' do
    %a{href: "/list?id=#{delivery.letter.id}"}= "Letter ##{delivery.letter.id}"
  = "\"#{delivery.letter.title}\""

%p
  = succeed ':' do
    %a{href: "/list?id=#{delivery.campaign.id}"}= "Letter ##{delivery.campaign.id}"
  = "\"#{delivery.campaign.title}\""

%p
  Details:

%pre= delivery.details

