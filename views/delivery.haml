%h2
  Delivery
  = succeed ':' do
    = "##{delivery.id}"

%p
  - recipient = delivery.recipient
  = succeed ':' do
    %a{href: "/recipient?id=#{recipient.id}&list=#{recipient.list.id}"}= "Recipient ##{recipient.id}"
  = "#{recipient.email}"

%p
  - letter = delivery.letter
  = succeed ':' do
    %a{href: "/list?id=#{letter.id}"}= "Letter ##{letter.id}"
  = "\"#{letter.title}\""

%p
  - campaign = delivery.campaign
  = succeed ':' do
    %a{href: "/campaign?id=#{campaign.id}"}= "Campaign ##{campaign.id}"
  = "\"#{campaign.title}\""

%p= delivery.details

%p
  %a.red{href: "/delete-delivery?id=#{delivery.id}", onclick: "return confirm('Are you sure?');"} Delete
